<% @sauce.errors.messages.each do | key, value | %>
 $('#sauce-form #<%= key %>').after(`<div class="invalid-tooltip"><%= value.join(' and ').prepend("#{key} ").concat('.').capitalize %></div>`);
 $('#sauce-form #<%= key %>').addClass('is-invalid');
<% end %>

