<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" id="hotsauced-logo">[LOGO HERE]</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav-bar">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="navbar-collapse collapse" id="nav-bar">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "HOME", home_path, id: 'nav-link-home', class: active?(home_path) %>
      </li>
      <li class="nav-item">
        <%= link_to "SAUCES", sauces_path, id: 'nav-link-sauces', class: active?(sauces_path) %>
      </li>
      <li class="nav-item">
        <%= link_to "ABOUT", about_path, id: 'nav-link-about', class: active?(about_path) %>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
          <%= inline_svg('icons8-contacts.svg', size: '32px', class: "img-fluid #{active?(user_path(current_user))}") %>
        </a>
        <div class="dropdown-menu dropdown-menu-right text-center px-3">
          <% if current_user %>
            <span>Hi, <%= current_user.username %></span>
            <div class="dropdown-divider"></div>
            <%= link_to 'Profile', user_path(current_user), class: 'dropdown-item text-dark' %>
            <%= link_to 'Sign Out', signout_path, class: 'dropdown-item text-dark' %>
          <% else %>
            <%= link_to 'Sign In', signin_path, id:'login', class: 'btn btn-outline-dark btn-sm btn-block' %>
            <div class="dropdown-divider"></div>
            <span>New User?</span>
            <%= link_to 'Create Account', new_user_path, class: 'text-dark text-center' %>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>
